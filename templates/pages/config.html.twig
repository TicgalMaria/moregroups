{#
#}

{% import 'components/form/fields_macros.html.twig' as fields %}

{% if options is not defined %}
{% set options = {} %}
{% endif %}

{{ include('components/form/header.html.twig', {'in_twig': true, 'no_header': true}) }}

<div class="row">
    <div class="row ps-4">

        {# As example, @see glpi/templates/components/form/fields_macros.html.twig
        {% if item.isField('password') %}
            {{ fields.passwordField(
                'password',
                item.fields['password'],
                __('Password'),
                options
            ) }}
        {% endif %}
        #}

    </div>
</div>

{{ include('components/form/buttons.html.twig') }}