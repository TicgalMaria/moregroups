{#
#}

{# The core base template provides the form, the hidden profile id, the CSRF
   token and the save button, so a plugin tab only fills in the content. #}
{% extends 'pages/admin/profile/base_tab.html.twig' %}

{% block content %}
    {% do item.displayRightsChoiceMatrix(rights, {canedit: rights_editable, title: title}) %}
{% endblock %}
